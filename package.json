{
  "name": "chartit",
  "version": "1.0.0",
  "description": "",
  "packageManager": "pnpm@9.1.4",
  "workspaces": [
    "apps/*",
    "packages/*",
    "configs/*"
  ],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "client": "pnpm --filter client",
    "ui": "pnpm --filter @chartit/ui",
    "build": "pnpm -r build"
  },
  "dependencies": {
    "react": "^18",
    "tailwindcss": "^3.4.1"
  },
  "devDependencies": {
    "eslint": "^8"
  }
}
